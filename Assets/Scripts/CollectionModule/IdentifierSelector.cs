using System;
using UnityEngine;

/**
 * Custom field, useful to show a custom field on strings.
 *
 * @see IdentifierSelectorDrawer
 */
[AttributeUsage(AttributeTargets.Field)]
public class IdentifierSelector : PropertyAttribute { }
